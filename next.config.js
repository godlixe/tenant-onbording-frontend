/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com',
            'portal.its.ac.id',
        ],
    },
};

module.exports = nextConfig;
